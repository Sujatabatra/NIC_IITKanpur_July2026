spring.application.name=config-server

server.port=8888

#spring.cloud.config.server.git.uri=file:///D:/microservices-config

#Uses local files instead of a Git repository.
spring.profiles.active=native

#Spring Cloud looks for configuration files under:
#src/main/resources/config
spring.cloud.config.server.native.search-locations=classpath:/config